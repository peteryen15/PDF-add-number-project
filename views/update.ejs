<!DOCTYPE html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PDF加編號</title>
    <link rel="stylesheet" href="./styles/style.css" />
  </head>
  <body>
    <div>
      <table>
        <form
          action="/update/update1"
          enctype="multipart/form-data"
          method="POST"
        >
          <tr>
            <td></td>
            <td>N95</td>
          </tr>
          <tr>
            <th>更改合約PDF</th>
            <td>
              <input type="file" accept="application/pdf" name="pdf1" />
              <% if (update_err_n95 != "") {%>
              <br /><strong><%= update_err_n95 %></strong>
              <% } %>
            </td>
          </tr>
          <tr>
            <td></td>
            <td><button class="update">更新 PDF</button></td>
          </tr>
        </form>
      </table>
      <table>
        <form
          action="/update/update2"
          enctype="multipart/form-data"
          method="POST"
        >
          <tr>
            <td>南區</td>
          </tr>
          <tr>
            <td>
              <input type="file" accept="application/pdf" name="pdf2" />
              <% if (update_err_south != "") {%>
              <br /><strong><%= update_err_south %></strong>
              <% } %>
            </td>
          </tr>
          <tr>
            <td><button class="update">更新 PDF</button></td>
          </tr>
        </form>
      </table>
      <table>
        <form
          action="/update/update3"
          enctype="multipart/form-data"
          method="POST"
        >
          <tr>
            <td>原制度</td>
          </tr>
          <tr>
            <td>
              <input type="file" accept="application/pdf" name="pdf3" />
              <% if (update_err_orig != "") {%>
              <br /><strong><%= update_err_orig %></strong>
              <% } %>
            </td>
          </tr>
          <tr>
            <td><button class="update">更新 PDF</button></td>
          </tr>
        </form>
      </table>
    </div>
    <div>
      <a href="/">回首頁</a>
    </div>
  </body>
</html>
